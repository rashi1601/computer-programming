#Split a string by a given delimiter.
def split_by_delimiter(s, delimiter):
    return s.split(delimiter)

# Example
print(split_by_delimiter("apple,banana,cherry", ","))  # Output: ['apple', 'banana', 'cherry']
