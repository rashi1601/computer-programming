#Find the difference between the largest and smallest element in a list.
def max_min_difference(lst):
    return max(lst) - min(lst)

# Example
print(max_min_difference([1, 2, 3, 4, 5]))  # Output: 4
